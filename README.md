# init-bun-svelte-elysia

Agent skill: scaffold Bun monorepo (SvelteKit + Elysia + Drizzle + Postgres).

## Install

```bash
npx skills add imampamuji/init-bun-svelte-elysia
```

Or copy this folder into `~/.agents/skills/init-bun-svelte-elysia/`.

